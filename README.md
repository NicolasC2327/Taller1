# Taller1
Hice una calculadora en python y en cada commit se presenta cada funcion que contiene(suma, resta, multiplicacion y division).

creé dos variables con los nombres "valorUno, ValorDos" e hice la opcion int(input(...) para agregar los valores a las variables en el terminal.

Tambien creé otra varible llamada valorTotal la cual se le depositara el valor de la operacion entre valorUno y valorDos.

Despues de que ya las variables valorUno y valorDos tengan valor hay un siguiente comando: "valorUno (+,-,*,/) valorDos = valorTotal" donde inicie la operacion.

Por ultimo, muestro el valor de la variable valorTotal con la funcion print(...) para que muestre el resultado de la ecuacion.
